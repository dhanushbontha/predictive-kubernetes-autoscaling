apiVersion: apps/v1
kind: Deployment
metadata:
  name: workload-service
  labels:
    app: workload-service
    app.kubernetes.io/name: workload-service
    app.kubernetes.io/part-of: predictive-kubernetes-autoscaling
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workload-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: workload-service
        app.kubernetes.io/name: workload-service
        app.kubernetes.io/part-of: predictive-kubernetes-autoscaling
    spec:
      containers:
        - name: workload-service
          image: predictive-k8s/workload-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8084
              protocol: TCP
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8084
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8084
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          env:
            - name: WORKLOAD_DEFAULT_ITERATIONS
              value: "10000000"
