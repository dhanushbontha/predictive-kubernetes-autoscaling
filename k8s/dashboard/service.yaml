apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: autoscaling-experiment
  labels:
    app: dashboard
    tier: frontend
spec:
  type: NodePort
  selector:
    app: dashboard
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
